{
  "$schema": "https://turborepo.com/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": ["dist/**"],
      "passThroughEnv": [
        "VITE_API_URL"
      ]
    },
    "start": {
      "passThroughEnv": [
        "PORT"
      ]
    }
  }
}
