{
  "name": "steamboat",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.2.5",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "types": "turbo run types --parallel"
  },
  "devDependencies": {
    "turbo": "^2.7.2"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  }
}
